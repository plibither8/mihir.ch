section#heading

	h1
		font-size: 40px
		padding: 10px 0
		color: var(--accent-color-9)
		font-family: var(--mono-font-stack)

	p.caption
		font-size: 18px
		color: #666

		em
			font-weight: bold
			font-size: 26px
			color: var(--accent-color-7)
			font-family: var(--mono-font-stack)
			font-style: normal

	img
		position: absolute
		width: 40vh
		right: -50px
		top: -50px
		opacity: 0.1
